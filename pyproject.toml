[project]
name = "nano-gpt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.3.0",
    "mlflow>=3.5.1",
    "numpy>=2.3.4",
    "torch>=2.9.0",
    "transformers>=4.57.1",
]
